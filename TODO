achievement_handler:
    Add page_size and page_number options on filter and range and sort options
    Multiple sorts, filter and ranges
    get_achievements_page_number_and_size with filter, sort, ranges
    Handle reverse sorts (-tier for example)
Cleanup of wiki for clearer navigation and same for .py files

Add the info token endpoint
Automated tests
Add in wiki links to file where function is located
DOC
remove `->`
Utility functions that will sort and return useful data on coalitions
GET /v2/campus/:campus_id/achievements in separate handler
GET /v2/cursus/:cursus_id/achievements in separate handler