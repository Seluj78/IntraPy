achievement_handler:
    Add page_size and page_number options on filter and range and sort options
    Multiple sorts, filter and ranges
    get_achievements_page_number_and_size with filter, sort, ranges
    Handle reverse sorts (-tier for example)

Add the info token endpoint
Automated tests
Add in wiki links to file where function is located